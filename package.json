{
  "name": "cryptiq",
  "private": true,
  "scripts": {
    "dev": "turbo run dev --filter=cryptiq",
    "build": "turbo run build --filter=cryptiq",
    "start": "turbo run start --filter=cryptiq",
    "lint": "turbo run lint --filter=cryptiq",
    "test": "turbo run test --filter=cryptiq"
  },
  "devDependencies": {
    "npm-run-all": "^4.1.5",
    "turbo": "^2.2.3",
    "typescript": "^5.6.3"
  },
  "packageManager": "pnpm@9.11.0",
  "dependencies": {
    "flowbite-react": "^0.10.2"
  }
}
